{
  "name": "ink-island",
  "version": "1.0.0",
  "description": "Ink Island — Paint, Claim, Conquer",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step needed — pure HTML'",
    "cap:add": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
